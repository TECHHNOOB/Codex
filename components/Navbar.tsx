'use client';
import { AnimatePresence, motion, useScroll, useMotionValueEvent } from 'framer-motion';
import { Menu, X } from 'lucide-react';
import { useState } from 'react';
const links=['Services','Work','Process','About','Contact'];
export default function Navbar(){ const [open,setOpen]=useState(false); const [scrolled,setScrolled]=useState(false); const {scrollY}=useScroll(); useMotionValueEvent(scrollY,'change',v=>setScrolled(v>20)); return <header className={`fixed inset-x-0 top-0 z-50 border-b transition-all duration-500 ${scrolled?'border-white/10 bg-burner-bg/75 backdrop-blur-xl':'border-transparent bg-transparent'}`}><div className="container flex h-20 items-center justify-between"><a href="#home" className="font-display text-xl font-bold tracking-tight">Burner<span className="text-burner-accent">media</span></a><nav className="hidden items-center gap-8 md:flex">{links.map(l=><a key={l} href={`#${l.toLowerCase()}`} className="link-underline text-sm text-white/75 hover:text-white">{l}</a>)}<a href="#contact" className="rounded-full bg-burner-accent px-5 py-3 text-sm font-bold text-white shadow-glow transition hover:-translate-y-0.5">Start a Project</a></nav><button onClick={()=>setOpen(!open)} className="md:hidden" aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div><AnimatePresence>{open&&<motion.div initial={{opacity:0,y:-20}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-20}} className="fixed inset-0 top-20 z-40 bg-burner-bg/95 p-6 backdrop-blur-xl md:hidden"><div className="grid gap-6 text-4xl font-display">{links.map(l=><a onClick={()=>setOpen(false)} href={`#${l.toLowerCase()}`} key={l}>{l}</a>)}<a onClick={()=>setOpen(false)} href="#contact" className="mt-4 rounded-full bg-burner-accent px-6 py-4 text-center text-lg">Start a Project</a></div></motion.div>}</AnimatePresence></header> }
